A successful branching model

<http://nvie.com/posts/a-successful-git-branching-model/>

Initiate branches:

    git flow init
    git flow init -d   # Default names

List all feature branches:

    git flow feature

Start and finish:

    git flow feature start mytest
    git flow feature finish mytest

Create a release:

    git flow release start relname
    git flow release finish relname
