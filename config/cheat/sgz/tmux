# Terminal multiplexer (like screen)

# start new with session name:
tmux
# attach:
tmux atmux attachtmux a -t session_id

# list sessions:
tmux ls

# kill session:
tmux kill-session -t session_id

# Detach from the current session:
Ctrl-b + d

# ## Windows/Panes

# Split window:
C-b %    # Split verticallyC-b "    # Split horizontally

# Resize pane:
C-b Ctrl-arrow   # Keep Ctrl pressedC-b Alt-arrow    # Same as Ctrl but faster

# Scroll:
C-b Page-UpC-b Page-Down

