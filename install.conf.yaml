- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.tmux.conf:
    ~/.config/nvim: config/nvim
    ~/.config/zsh: config/zsh

