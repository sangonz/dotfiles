- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitalias:
    ~/.tmux.conf:
    ~/.zshrc:
    ~/.SpaceVim:
    ~/.SpaceVim.d:
    ~/.vim:
      path: ~/.SpaceVim
      relative: true
    ~/.config/zsh: config/zsh

